# Where the API lives. Read at BUILD time (Vite), so a change needs a rebuild/redeploy.
# Local dev: omit it entirely — the code falls back to http://localhost:4000.
# Vercel: set it in the project's environment variables.
VITE_API_URL=https://api.orin.ashutoshsao.com
